{
  "version": 2,
  "builds": [
    {
      "src": "index.js",  // Use the correct entry file (index.js in your case)
      "use": "@vercel/node"
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/index.js"  // Route all requests to index.js
    }
  ]
}
